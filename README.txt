# OpenGl_Triangles

This program will compile on Linux, Mac, and Windows.

To compile use gcc and have the required glut libraries.

command line arguments: gcc triangles.c -o triangles -lGL -lGLU -lglut
